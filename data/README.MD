`output/` folder contains the output of the data processing pipeline.

`dataset_description.txt` contains a description of the dataset.
`schema_example.csv` contains a sample of the dataset.
`chess_moves_dataset.csv` contains the dataset in CSV format.
